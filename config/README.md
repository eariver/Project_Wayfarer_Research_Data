# Configuration

This directory contains version-controlled collector and source configuration.

The bootstrap branch includes only an example polling panel. A production panel must be reviewed before Phase A begins and should identify the calibration phase, fixed schedule, stable server IDs, endpoints, and source categories.

Configuration changes affect comparability. A panel must remain fixed during a calibration phase unless a documented acquisition defect requires a correction.
