# Samples

This directory contains synthetic, non-production examples used to validate collector contracts and tooling.

- Sample endpoints use reserved `.invalid` names.
- Sample player counts, scores, votes, and timestamps are fictional.
- Files under this directory must never be treated as observed market data.
- Production collectors write only under `raw/`.
